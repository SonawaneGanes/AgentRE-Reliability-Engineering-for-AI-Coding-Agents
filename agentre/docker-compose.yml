services:
  agentre:
    build: .
    environment:
      LLM_PROVIDER: ${LLM_PROVIDER:-ollama}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      OLLAMA_MODEL: ${OLLAMA_MODEL:-CHANGE_ME}
    extra_hosts:
      - "host.docker.internal:host-gateway"
